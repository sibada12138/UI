# 2026-04-22 未完成差距与执行 TODO

## 目标
- 补齐 `API对接系统.txt` 中未落地的充值链路对接。
- 确保前端有可用的“发送短信验证码”按钮与倒计时流程。
- 确保后台充值流程不是“假链接”，而是可切换到真实外部接口链路。

## TODO 列表（逐项执行）

1. [x] 差距盘点并固化 TODO  
差距：
- 充值任务 `generate-link` 仅生成本地伪链接，未对接外部充值接口。
- 后台未提供渠道配置（联想/网页/Android）与渠道维护。
- 待办中心未展示外部返回的充值链接/二维码详细信息。
- 前端短信按钮已存在，但需与后端接口一致性再核验，避免“按钮在但流程不通”。

2. [x] 后端充值对接补齐  
- 增加渠道配置读取/写入（本地文件持久化）。
- 增加外部充值调用链（VIP 查询 -> 创建订单 -> 收银台协议二维码）。
- `generate-link` 支持两种模式：
  - 本地模式（fallback）
  - 外部 API 模式（传入 accessToken/cookie/channel）

3. [x] 管理端待办中心联动  
- 展示并复制完整充值链接。
- 展示二维码预览（data URL）与外部订单原始信息摘要。
- 提供渠道选择和外部模式触发入口。

4. [x] 短信发送流程核验  
- 前端按钮 -> `/api/public/token/send-sms`  
- 后端返回 `retryAfterSec`，前端倒计时统一显示。

5. [x] 验证与交付  
- `pnpm --filter api build`
- `pnpm --filter api test -- --runInBand`
- `pnpm --filter web lint`
- `pnpm --filter web exec tsc --noEmit`
